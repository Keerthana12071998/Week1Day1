const browserVersion = "Chrome"
function getBrowserVersion(browserVersion)
{
    if(browserVersion === "Chrome")
    {
        //var browserVersion = "new"
        let browserVersion = "new"
    }
    console.log(browserVersion)
}
getBrowserVersion(browserVersion)